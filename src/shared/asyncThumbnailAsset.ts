/** @deprecated Import from `@/shared/formatCapabilities` — re-exported for existing imports. */
export {
  isDeferredThumbnailExtension as isDeferredThumbnailAsset,
  shouldRenderThumbnailSlot as shouldRenderThumbnailImage,
  shouldRetryThumbnailWhileEmpty
} from './formatCapabilities'
